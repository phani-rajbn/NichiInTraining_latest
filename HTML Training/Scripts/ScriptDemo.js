//alert("Test123");
//JS is executed thro an interpreter which is found inside the browser. 
//var empName
//empName = prompt("Enter the name","Title");
//alert(empName)
var empAddress ="Bangalore"
var age = 41;
//var is a universal data type of JS. Any variable that U  wish to store will be of the type var. But it internally stores the data as number, text, date, bool and float. If a number is not representable as a numeric expression it is a NaN(Not a Number).
//Text will be assigned either with single quote or double quote 
age++;
//alert("Current age is " + age)
//alert(empAddress);

function testFunc(){
	empSalary = 25000;//Scope is local to this function...
}	

//4 Functions for mathematic operations:
function addFunc(v1, v2){
	return v1 + v2
}
function subFunc(v1, v2){
	return v1 - v2
}
function mulFunc(v1, v2){
	return v1 * v2
}
function divFunc(v1, v2){
	return v1 / v2
}

